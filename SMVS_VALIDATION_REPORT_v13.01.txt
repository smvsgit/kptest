KARYALAY PORTAL v13.01 - DEPLOYMENT FIX VALIDATION REPORT
Date: 2026-09-08
Release: Coolify npm-ci Deployment Fix
Previous: 13.00

ROOT CAUSE FROM COOLIFY LOG
- Docker frontend assets stage failed at: npm ci --no-audit --no-fund --ignore-scripts
- npm 10.9.x reported package-lock peer-resolution mismatch around optional WASM dependencies:
  @emnapi/core, @emnapi/runtime, and @emnapi/wasi-threads.
- This is a lockfile peer-resolution/platform metadata issue, not a Laravel/PHP application error.

FIX
- Added project .npmrc with legacy-peer-deps=true.
- Dockerfile still uses npm ci, so production continues to install from package-lock.json.
- The setting only relaxes peer-dependency re-resolution; it does not switch the build to npm install.
- Existing media bind mounts remain configured identically for app, worker, and scheduler:
  /srv/media/projects/karyalayportal/uploads:/var/www/html/storage/app/media/uploads

VALIDATION PERFORMED
- npm version in validation environment: 10.9.2; Node: 22.16.0.
- npm config legacy-peer-deps => true.
- npm ci --no-audit --no-fund --ignore-scripts --dry-run => PASS.
- 161 PHP files syntax linted => PASS.
- package.json / package-lock.json JSON parse => PASS.
- docker-compose.yml YAML parse => PASS.
- Version markers => VERSION 13.01; config current 13.01; previous 13.00.
- app/worker/scheduler all contain the required host media bind mount.
- vendor/ and node_modules/ are not shipped in source ZIP.

LIMITATION
- This execution environment cannot perform the full networked Docker build against npm/Docker registries.
- The exact failure gate that previously stopped Coolify (npm ci metadata validation) was reproduced locally before the fix and now passes with the project .npmrc.
- Final confirmation is the next Coolify redeploy of this exact v13.01 artifact/repository content.
