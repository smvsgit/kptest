# npm v10+ can re-resolve peer dependencies in platform-specific optional WASM
# packages differently from the lockfile generated on another platform.
# Keep `npm ci` deterministic for the Linux production build while preserving
# the lockfile's selected native Linux packages.
legacy-peer-deps=true
audit=false
fund=false
