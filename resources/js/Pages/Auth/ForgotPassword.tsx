import { FormEvent } from 'react';
import { Link, useForm } from '@inertiajs/react';
export default function ForgotPassword(){ const f=useForm({email:''}); const submit=(e:FormEvent)=>{e.preventDefault();f.post('/forgot-password')}; return <div className="auth-container"><div className="auth-card"><h1 className="auth-title">Reset password</h1><p className="auth-subtitle">Enter your account email. Active accounts receive a single-use reset link.</p><form onSubmit={submit} className="auth-form"><div className="auth-field"><label htmlFor="email">Email address</label><input id="email" type="email" value={f.data.email} onChange={e=>f.setData('email',e.target.value)} autoFocus/>{f.errors.email&&<span className="auth-error">{f.errors.email}</span>}</div><button className="auth-submit" disabled={f.processing}>{f.processing?'Sending…':'Send reset link'}</button></form><p className="auth-footer-text"><Link className="auth-link" href="/login">Back to sign in</Link></p></div></div> }
