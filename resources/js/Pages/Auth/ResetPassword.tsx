import { FormEvent } from 'react';
import { useForm } from '@inertiajs/react';
export default function ResetPassword({token,email}:{token:string,email:string}){const f=useForm({email:email||'',password:'',password_confirmation:''});const submit=(e:FormEvent)=>{e.preventDefault();f.post(`/reset-password/${encodeURIComponent(token)}`)};return <div className="auth-container"><div className="auth-card"><h1 className="auth-title">Choose a new password</h1><form onSubmit={submit} className="auth-form"><div className="auth-field"><label>Email</label><input type="email" value={f.data.email} onChange={e=>f.setData('email',e.target.value)}/></div><div className="auth-field"><label>New password</label><input type="password" value={f.data.password} onChange={e=>f.setData('password',e.target.value)}/></div><div className="auth-field"><label>Confirm password</label><input type="password" value={f.data.password_confirmation} onChange={e=>f.setData('password_confirmation',e.target.value)}/></div>{Object.values(f.errors).map((e,i)=><span className="auth-error" key={i}>{e}</span>)}<button className="auth-submit" disabled={f.processing}>Reset password</button></form></div></div>}
